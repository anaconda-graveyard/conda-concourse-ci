label: centos5-64
platform: linux
arch: 64
connector:
  image_resource:
    type: docker-image
    check_every: 1h
    source:
      repository: busybox
      username: ((common.dockerhub-user))
      password: ((common.dockerhub-pass))
