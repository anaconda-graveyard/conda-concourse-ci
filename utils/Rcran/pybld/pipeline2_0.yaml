groups: []
resources:
- name: rsync-build-pack
  type: rsync-resource
  source:
    base_dir: /ci/build_pack
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script0-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script0-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script0-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script1-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script1-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script1-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script2-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script2-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script2-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script3-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script3-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script3-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script4-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script4-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script4-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script5-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script5-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script5-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script6-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script6-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script6-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script7-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script7-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script7-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script8-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script8-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script8-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script9-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script9-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_py_script9-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts_py
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
resource_types:
- name: rsync-resource
  type: docker-image
  source:
    repository: conda/concourse-rsync-resource
    tag: latest
jobs:
- name: build_py_script0-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          apispec-feedstock
          automat-feedstock
          autopep8-feedstock
          babel-feedstock
          backports.weakref-feedstock
          chest-feedstock
          click-plugins-feedstock
          commonmark-feedstock
          conda-package-handling-feedstock
          dbf-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          -c local --output-folder=output-py_artifacts --cache-dir=output-source --stats-file=stats/build_py_script0-on-linux_64_1564756033.json
          --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/apispec-feedstock
          aggregate/automat-feedstock
          aggregate/autopep8-feedstock
          aggregate/babel-feedstock
          aggregate/backports.weakref-feedstock
          aggregate/chest-feedstock
          aggregate/click-plugins-feedstock
          aggregate/commonmark-feedstock
          aggregate/conda-package-handling-feedstock
          aggregate/dbf-feedstock
          
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script0-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script0-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          apispec-feedstock
          automat-feedstock
          autopep8-feedstock
          babel-feedstock
          backports.weakref-feedstock
          chest-feedstock
          click-plugins-feedstock
          commonmark-feedstock
          conda-package-handling-feedstock
          dbf-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script0-on-osx_1564756033.json
          -c local --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/apispec-feedstock
          aggregate/automat-feedstock
          aggregate/autopep8-feedstock
          aggregate/babel-feedstock
          aggregate/backports.weakref-feedstock
          aggregate/chest-feedstock
          aggregate/click-plugins-feedstock
          aggregate/commonmark-feedstock
          aggregate/conda-package-handling-feedstock
          aggregate/dbf-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script0-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script0-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          apispec-feedstock
          automat-feedstock
          autopep8-feedstock
          babel-feedstock
          backports.weakref-feedstock
          chest-feedstock
          click-plugins-feedstock
          commonmark-feedstock
          conda-package-handling-feedstock
          dbf-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script08-on-winbuilder_1564756033.json
          --croot C:\ci -c local -m aggregate/conda_build_config.yaml
          aggregate/apispec-feedstock
          aggregate/automat-feedstock
          aggregate/autopep8-feedstock
          aggregate/babel-feedstock
          aggregate/backports.weakref-feedstock
          aggregate/chest-feedstock
          aggregate/click-plugins-feedstock
          aggregate/commonmark-feedstock
          aggregate/conda-package-handling-feedstock
          aggregate/dbf-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script0-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script1-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          docker-pycreds-feedstock
          execnet-feedstock
          fasteners-feedstock
          gitdb2-feedstock
          google-pasta-feedstock
          html5lib-feedstock
          hyperlink-feedstock
          isodate-feedstock
          jaraco.classes-feedstock
          ldap3-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          -c local --output-folder=output-py_artifacts --cache-dir=output-source --stats-file=stats/build_py_script1-on-linux_64_1564756033.json
          --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/docker-pycreds-feedstock
          aggregate/execnet-feedstock
          aggregate/fasteners-feedstock
          aggregate/gitdb2-feedstock
          aggregate/google-pasta-feedstock
          aggregate/html5lib-feedstock
          aggregate/hyperlink-feedstock
          aggregate/isodate-feedstock
          aggregate/jaraco.classes-feedstock
          aggregate/ldap3-feedstock
          
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script1-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script1-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          docker-pycreds-feedstock
          execnet-feedstock
          fasteners-feedstock
          gitdb2-feedstock
          google-pasta-feedstock
          html5lib-feedstock
          hyperlink-feedstock
          isodate-feedstock
          jaraco.classes-feedstock
          ldap3-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script1-on-osx_1564756033.json
          -c local --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/docker-pycreds-feedstock
          aggregate/execnet-feedstock
          aggregate/fasteners-feedstock
          aggregate/gitdb2-feedstock
          aggregate/google-pasta-feedstock
          aggregate/html5lib-feedstock
          aggregate/hyperlink-feedstock
          aggregate/isodate-feedstock
          aggregate/jaraco.classes-feedstock
          aggregate/ldap3-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script1-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script1-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          docker-pycreds-feedstock
          execnet-feedstock
          fasteners-feedstock
          gitdb2-feedstock
          google-pasta-feedstock
          html5lib-feedstock
          hyperlink-feedstock
          isodate-feedstock
          jaraco.classes-feedstock
          ldap3-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script18-on-winbuilder_1564756033.json
          --croot C:\ci -c local -m aggregate/conda_build_config.yaml
          aggregate/docker-pycreds-feedstock
          aggregate/execnet-feedstock
          aggregate/fasteners-feedstock
          aggregate/gitdb2-feedstock
          aggregate/google-pasta-feedstock
          aggregate/html5lib-feedstock
          aggregate/hyperlink-feedstock
          aggregate/isodate-feedstock
          aggregate/jaraco.classes-feedstock
          aggregate/ldap3-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script1-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script2-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          mako-feedstock
          memory_profiler-feedstock
          meson-feedstock
          msgpack-numpy-feedstock
          mypy-feedstock
          openpyxl-feedstock
          opentracing_instrumentation-feedstock
          opt_einsum-feedstock
          packaging-feedstock
          partd-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          -c local --output-folder=output-py_artifacts --cache-dir=output-source --stats-file=stats/build_py_script2-on-linux_64_1564756033.json
          --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/mako-feedstock
          aggregate/memory_profiler-feedstock
          aggregate/meson-feedstock
          aggregate/msgpack-numpy-feedstock
          aggregate/mypy-feedstock
          aggregate/openpyxl-feedstock
          aggregate/opentracing_instrumentation-feedstock
          aggregate/opt_einsum-feedstock
          aggregate/packaging-feedstock
          aggregate/partd-feedstock
          
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script2-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script2-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          mako-feedstock
          memory_profiler-feedstock
          meson-feedstock
          msgpack-numpy-feedstock
          mypy-feedstock
          openpyxl-feedstock
          opentracing_instrumentation-feedstock
          opt_einsum-feedstock
          packaging-feedstock
          partd-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script2-on-osx_1564756033.json
          -c local --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/mako-feedstock
          aggregate/memory_profiler-feedstock
          aggregate/meson-feedstock
          aggregate/msgpack-numpy-feedstock
          aggregate/mypy-feedstock
          aggregate/openpyxl-feedstock
          aggregate/opentracing_instrumentation-feedstock
          aggregate/opt_einsum-feedstock
          aggregate/packaging-feedstock
          aggregate/partd-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script2-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script2-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          mako-feedstock
          memory_profiler-feedstock
          meson-feedstock
          msgpack-numpy-feedstock
          mypy-feedstock
          openpyxl-feedstock
          opentracing_instrumentation-feedstock
          opt_einsum-feedstock
          packaging-feedstock
          partd-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script28-on-winbuilder_1564756033.json
          --croot C:\ci -c local -m aggregate/conda_build_config.yaml
          aggregate/mako-feedstock
          aggregate/memory_profiler-feedstock
          aggregate/meson-feedstock
          aggregate/msgpack-numpy-feedstock
          aggregate/mypy-feedstock
          aggregate/openpyxl-feedstock
          aggregate/opentracing_instrumentation-feedstock
          aggregate/opt_einsum-feedstock
          aggregate/packaging-feedstock
          aggregate/partd-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script2-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script3-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          pyaml-feedstock
          pyasn1-modules-feedstock
          pyct-core-feedstock
          pydocstyle-feedstock
          pydotplus-feedstock
          pyhamcrest-feedstock
          pysmi-feedstock
          python-dateutil-feedstock
          python-engineio-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          -c local --output-folder=output-py_artifacts --cache-dir=output-source --stats-file=stats/build_py_script3-on-linux_64_1564756033.json
          --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/perf-feedstock
          aggregate/pyaml-feedstock
          aggregate/pyasn1-modules-feedstock
          aggregate/pyct-core-feedstock
          aggregate/pydocstyle-feedstock
          aggregate/pydotplus-feedstock
          aggregate/pyhamcrest-feedstock
          aggregate/pysmi-feedstock
          aggregate/python-dateutil-feedstock
          aggregate/python-engineio-feedstock
          
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script3-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script3-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          pyaml-feedstock
          pyasn1-modules-feedstock
          pyct-core-feedstock
          pydocstyle-feedstock
          pydotplus-feedstock
          pyhamcrest-feedstock
          pysmi-feedstock
          python-dateutil-feedstock
          python-engineio-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script3-on-osx_1564756033.json
          -c local --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/perf-feedstock
          aggregate/pyaml-feedstock
          aggregate/pyasn1-modules-feedstock
          aggregate/pyct-core-feedstock
          aggregate/pydocstyle-feedstock
          aggregate/pydotplus-feedstock
          aggregate/pyhamcrest-feedstock
          aggregate/pysmi-feedstock
          aggregate/python-dateutil-feedstock
          aggregate/python-engineio-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script3-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script3-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          pyaml-feedstock
          pyasn1-modules-feedstock
          pyct-core-feedstock
          pydocstyle-feedstock
          pydotplus-feedstock
          pyhamcrest-feedstock
          pysmi-feedstock
          python-dateutil-feedstock
          python-engineio-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script38-on-winbuilder_1564756033.json
          --croot C:\ci -c local -m aggregate/conda_build_config.yaml
          aggregate/perf-feedstock
          aggregate/pyaml-feedstock
          aggregate/pyasn1-modules-feedstock
          aggregate/pyct-core-feedstock
          aggregate/pydocstyle-feedstock
          aggregate/pydotplus-feedstock
          aggregate/pyhamcrest-feedstock
          aggregate/pysmi-feedstock
          aggregate/python-dateutil-feedstock
          aggregate/python-engineio-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script3-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script4-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          python-jose-feedstock
          python-slugify-feedstock
          pyviz_comms-feedstock
          qtawesome-feedstock
          retrying-feedstock
          snuggs-feedstock
          sortedcollections-feedstock
          sphinxcontrib-websupport-feedstock
          threadloop-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          -c local --output-folder=output-py_artifacts --cache-dir=output-source --stats-file=stats/build_py_script4-on-linux_64_1564756033.json
          --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/python-jose-feedstock
          aggregate/python-slugify-feedstock
          aggregate/pyviz_comms-feedstock
          aggregate/qtawesome-feedstock
          aggregate/retrying-feedstock
          aggregate/sas7bdat-feedstock
          aggregate/snuggs-feedstock
          aggregate/sortedcollections-feedstock
          aggregate/sphinxcontrib-websupport-feedstock
          aggregate/threadloop-feedstock
          
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script4-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script4-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          python-jose-feedstock
          python-slugify-feedstock
          pyviz_comms-feedstock
          qtawesome-feedstock
          retrying-feedstock
          snuggs-feedstock
          sortedcollections-feedstock
          sphinxcontrib-websupport-feedstock
          threadloop-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script4-on-osx_1564756033.json
          -c local --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/python-jose-feedstock
          aggregate/python-slugify-feedstock
          aggregate/pyviz_comms-feedstock
          aggregate/qtawesome-feedstock
          aggregate/retrying-feedstock
          aggregate/sas7bdat-feedstock
          aggregate/snuggs-feedstock
          aggregate/sortedcollections-feedstock
          aggregate/sphinxcontrib-websupport-feedstock
          aggregate/threadloop-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script4-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script4-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          python-jose-feedstock
          python-slugify-feedstock
          pyviz_comms-feedstock
          qtawesome-feedstock
          retrying-feedstock
          snuggs-feedstock
          sortedcollections-feedstock
          sphinxcontrib-websupport-feedstock
          threadloop-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script48-on-winbuilder_1564756033.json
          --croot C:\ci -c local -m aggregate/conda_build_config.yaml
          aggregate/python-jose-feedstock
          aggregate/python-slugify-feedstock
          aggregate/pyviz_comms-feedstock
          aggregate/qtawesome-feedstock
          aggregate/retrying-feedstock
          aggregate/sas7bdat-feedstock
          aggregate/snuggs-feedstock
          aggregate/sortedcollections-feedstock
          aggregate/sphinxcontrib-websupport-feedstock
          aggregate/threadloop-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script4-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script5-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          visvis-feedstock
          zict-feedstock
          absl-py-feedstock
          asyncpgsa-feedstock
          atom-feedstock
          backports.lzma-feedstock
          backports.os-feedstock
          backports.shutil_get_terminal_size-feedstock
          backports.shutil_which-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          -c local --output-folder=output-py_artifacts --cache-dir=output-source --stats-file=stats/build_py_script5-on-linux_64_1564756033.json
          --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/visvis-feedstock
          aggregate/w3lib-feedstock
          aggregate/zict-feedstock
          aggregate/absl-py-feedstock
          aggregate/asyncpgsa-feedstock
          aggregate/atom-feedstock
          aggregate/backports.lzma-feedstock
          aggregate/backports.os-feedstock
          aggregate/backports.shutil_get_terminal_size-feedstock
          aggregate/backports.shutil_which-feedstock
          
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script5-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script5-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          visvis-feedstock
          zict-feedstock
          absl-py-feedstock
          asyncpgsa-feedstock
          atom-feedstock
          backports.lzma-feedstock
          backports.os-feedstock
          backports.shutil_get_terminal_size-feedstock
          backports.shutil_which-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script5-on-osx_1564756033.json
          -c local --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/visvis-feedstock
          aggregate/w3lib-feedstock
          aggregate/zict-feedstock
          aggregate/absl-py-feedstock
          aggregate/asyncpgsa-feedstock
          aggregate/atom-feedstock
          aggregate/backports.lzma-feedstock
          aggregate/backports.os-feedstock
          aggregate/backports.shutil_get_terminal_size-feedstock
          aggregate/backports.shutil_which-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script5-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script5-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          visvis-feedstock
          zict-feedstock
          absl-py-feedstock
          asyncpgsa-feedstock
          atom-feedstock
          backports.lzma-feedstock
          backports.os-feedstock
          backports.shutil_get_terminal_size-feedstock
          backports.shutil_which-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script58-on-winbuilder_1564756033.json
          --croot C:\ci -c local -m aggregate/conda_build_config.yaml
          aggregate/visvis-feedstock
          aggregate/w3lib-feedstock
          aggregate/zict-feedstock
          aggregate/absl-py-feedstock
          aggregate/asyncpgsa-feedstock
          aggregate/atom-feedstock
          aggregate/backports.lzma-feedstock
          aggregate/backports.os-feedstock
          aggregate/backports.shutil_get_terminal_size-feedstock
          aggregate/backports.shutil_which-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script5-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script6-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          biopython-feedstock
          bottleneck-feedstock
          capturer-feedstock
          cffi-feedstock
          cftime-feedstock
          cligj-feedstock
          coloredlogs-feedstock
          cycler-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          -c local --output-folder=output-py_artifacts --cache-dir=output-source --stats-file=stats/build_py_script6-on-linux_64_1564756033.json
          --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/biopython-feedstock
          aggregate/bottleneck-feedstock
          aggregate/capturer-feedstock
          aggregate/cffi-feedstock
          aggregate/cftime-feedstock
          aggregate/cligj-feedstock
          aggregate/colander-feedstock
          aggregate/coloredlogs-feedstock
          aggregate/configobj-feedstock
          aggregate/cycler-feedstock
          
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script6-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script6-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          biopython-feedstock
          bottleneck-feedstock
          capturer-feedstock
          cffi-feedstock
          cftime-feedstock
          cligj-feedstock
          coloredlogs-feedstock
          cycler-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script6-on-osx_1564756033.json
          -c local --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/biopython-feedstock
          aggregate/bottleneck-feedstock
          aggregate/capturer-feedstock
          aggregate/cffi-feedstock
          aggregate/cftime-feedstock
          aggregate/cligj-feedstock
          aggregate/colander-feedstock
          aggregate/coloredlogs-feedstock
          aggregate/configobj-feedstock
          aggregate/cycler-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script6-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script6-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          biopython-feedstock
          bottleneck-feedstock
          capturer-feedstock
          cffi-feedstock
          cftime-feedstock
          cligj-feedstock
          coloredlogs-feedstock
          cycler-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script68-on-winbuilder_1564756033.json
          --croot C:\ci -c local -m aggregate/conda_build_config.yaml
          aggregate/biopython-feedstock
          aggregate/bottleneck-feedstock
          aggregate/capturer-feedstock
          aggregate/cffi-feedstock
          aggregate/cftime-feedstock
          aggregate/cligj-feedstock
          aggregate/colander-feedstock
          aggregate/coloredlogs-feedstock
          aggregate/configobj-feedstock
          aggregate/cycler-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script6-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script7-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          cytoolz-feedstock
          django-feedstock
          doit-feedstock
          fast-histogram-feedstock
          gdal-feedstock
          h2-feedstock
          h5py-feedstock
          hdmedians-feedstock
          httpretty-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          -c local --output-folder=output-py_artifacts --cache-dir=output-source --stats-file=stats/build_py_script7-on-linux_64_1564756033.json
          --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/cytoolz-feedstock
          aggregate/django-feedstock
          aggregate/doit-feedstock
          aggregate/expressions-feedstock
          aggregate/fast-histogram-feedstock
          aggregate/gdal-feedstock
          aggregate/h2-feedstock
          aggregate/h5py-feedstock
          aggregate/hdmedians-feedstock
          aggregate/httpretty-feedstock
          
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script7-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script7-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          cytoolz-feedstock
          django-feedstock
          doit-feedstock
          fast-histogram-feedstock
          gdal-feedstock
          h2-feedstock
          h5py-feedstock
          hdmedians-feedstock
          httpretty-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script7-on-osx_1564756033.json
          -c local --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/cytoolz-feedstock
          aggregate/django-feedstock
          aggregate/doit-feedstock
          aggregate/expressions-feedstock
          aggregate/fast-histogram-feedstock
          aggregate/gdal-feedstock
          aggregate/h2-feedstock
          aggregate/h5py-feedstock
          aggregate/hdmedians-feedstock
          aggregate/httpretty-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script7-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script7-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          cytoolz-feedstock
          django-feedstock
          doit-feedstock
          fast-histogram-feedstock
          gdal-feedstock
          h2-feedstock
          h5py-feedstock
          hdmedians-feedstock
          httpretty-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script78-on-winbuilder_1564756033.json
          --croot C:\ci -c local -m aggregate/conda_build_config.yaml
          aggregate/cytoolz-feedstock
          aggregate/django-feedstock
          aggregate/doit-feedstock
          aggregate/expressions-feedstock
          aggregate/fast-histogram-feedstock
          aggregate/gdal-feedstock
          aggregate/h2-feedstock
          aggregate/h5py-feedstock
          aggregate/hdmedians-feedstock
          aggregate/httpretty-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script7-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script8-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          hypothesis-feedstock
          idna_ssl-feedstock
          importlib_metadata-feedstock
          intervals-feedstock
          jaraco.functools-feedstock
          jedi-feedstock
          leather-feedstock
          mkl_fft-feedstock
          mkl_random-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          -c local --output-folder=output-py_artifacts --cache-dir=output-source --stats-file=stats/build_py_script8-on-linux_64_1564756033.json
          --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/hypothesis-feedstock
          aggregate/idna_ssl-feedstock
          aggregate/importlib_metadata-feedstock
          aggregate/intervals-feedstock
          aggregate/jaraco.functools-feedstock
          aggregate/jedi-feedstock
          aggregate/leather-feedstock
          aggregate/mdp-feedstock
          aggregate/mkl_fft-feedstock
          aggregate/mkl_random-feedstock
          
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script8-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script8-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          hypothesis-feedstock
          idna_ssl-feedstock
          importlib_metadata-feedstock
          intervals-feedstock
          jaraco.functools-feedstock
          jedi-feedstock
          leather-feedstock
          mkl_fft-feedstock
          mkl_random-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script8-on-osx_1564756033.json
          -c local --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/hypothesis-feedstock
          aggregate/idna_ssl-feedstock
          aggregate/importlib_metadata-feedstock
          aggregate/intervals-feedstock
          aggregate/jaraco.functools-feedstock
          aggregate/jedi-feedstock
          aggregate/leather-feedstock
          aggregate/mdp-feedstock
          aggregate/mkl_fft-feedstock
          aggregate/mkl_random-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script8-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script8-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          hypothesis-feedstock
          idna_ssl-feedstock
          importlib_metadata-feedstock
          intervals-feedstock
          jaraco.functools-feedstock
          jedi-feedstock
          leather-feedstock
          mkl_fft-feedstock
          mkl_random-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script88-on-winbuilder_1564756033.json
          --croot C:\ci -c local -m aggregate/conda_build_config.yaml
          aggregate/hypothesis-feedstock
          aggregate/idna_ssl-feedstock
          aggregate/importlib_metadata-feedstock
          aggregate/intervals-feedstock
          aggregate/jaraco.functools-feedstock
          aggregate/jedi-feedstock
          aggregate/leather-feedstock
          aggregate/mdp-feedstock
          aggregate/mkl_fft-feedstock
          aggregate/mkl_random-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script8-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script9-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          nltk-feedstock
          numba-feedstock
          numexpr-feedstock
          orderedmultidict-feedstock
          pathlib2-feedstock
          pexpect-feedstock
          pillow-feedstock
          preshed-feedstock
          protobuf-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          -c local --output-folder=output-py_artifacts --cache-dir=output-source --stats-file=stats/build_py_script9-on-linux_64_1564756033.json
          --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/neotime-feedstock
          aggregate/nltk-feedstock
          aggregate/numba-feedstock
          aggregate/numexpr-feedstock
          aggregate/orderedmultidict-feedstock
          aggregate/pathlib2-feedstock
          aggregate/pexpect-feedstock
          aggregate/pillow-feedstock
          aggregate/preshed-feedstock
          aggregate/protobuf-feedstock
          
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script9-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script9-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          nltk-feedstock
          numba-feedstock
          numexpr-feedstock
          orderedmultidict-feedstock
          pathlib2-feedstock
          pexpect-feedstock
          pillow-feedstock
          preshed-feedstock
          protobuf-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script9-on-osx_1564756033.json
          -c local --croot . -m ./aggregate/conda_build_config.yaml
          aggregate/neotime-feedstock
          aggregate/nltk-feedstock
          aggregate/numba-feedstock
          aggregate/numexpr-feedstock
          aggregate/orderedmultidict-feedstock
          aggregate/pathlib2-feedstock
          aggregate/pexpect-feedstock
          aggregate/pillow-feedstock
          aggregate/preshed-feedstock
          aggregate/protobuf-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script9-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
- name: build_py_script9-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregate.git &&
          cd aggregate && git submodule update --init 
          nltk-feedstock
          numba-feedstock
          numexpr-feedstock
          orderedmultidict-feedstock
          pathlib2-feedstock
          pexpect-feedstock
          pillow-feedstock
          preshed-feedstock
          protobuf-feedstock
          && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-py_artifacts
          --python=3.8
          --cache-dir=output-source --stats-file=stats/build_py_script98-on-winbuilder_1564756033.json
          --croot C:\ci -c local -m aggregate/conda_build_config.yaml
          aggregate/neotime-feedstock
          aggregate/nltk-feedstock
          aggregate/numba-feedstock
          aggregate/numexpr-feedstock
          aggregate/orderedmultidict-feedstock
          aggregate/pathlib2-feedstock
          aggregate/pexpect-feedstock
          aggregate/pillow-feedstock
          aggregate/preshed-feedstock
          aggregate/protobuf-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-py_artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_py_script9-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-py_artifacts
    get_params:
      skip_download: true
