groups: []
resources:
- name: rsync-build-pack
  type: rsync-resource
  source:
    base_dir: /ci/build_pack
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script0-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script0-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script0-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script1-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script1-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script1-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script2-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script2-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script2-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script3-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script3-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script3-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script4-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script4-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script4-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script5-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script5-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script5-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script6-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script6-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script6-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script7-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script7-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script7-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script8-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script8-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script8-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script9-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script9-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script9-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
resource_types:
- name: rsync-resource
  type: docker-image
  source:
    repository: conda/concourse-rsync-resource
    tag: latest
jobs:
- name: build_r_script0-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script0-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-globalopttests-feedstock
          aggregateR/r-glpkapi-feedstock
          aggregateR/r-gmat-feedstock
          aggregateR/r-gmcm-feedstock
          aggregateR/r-gmeta-feedstock
          aggregateR/r-gmse-feedstock
          aggregateR/r-gmwt-feedstock
          aggregateR/r-gnmf-feedstock
          aggregateR/r-goftest-feedstock
          aggregateR/r-goftte-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script0-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script0-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script0-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-globalopttests-feedstock
          aggregateR/r-glpkapi-feedstock
          aggregateR/r-gmat-feedstock
          aggregateR/r-gmcm-feedstock
          aggregateR/r-gmeta-feedstock
          aggregateR/r-gmse-feedstock
          aggregateR/r-gmwt-feedstock
          aggregateR/r-gnmf-feedstock
          aggregateR/r-goftest-feedstock
          aggregateR/r-goftte-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script0-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script0-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script08-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-globalopttests-feedstock
          aggregateR/r-glpkapi-feedstock
          aggregateR/r-gmat-feedstock
          aggregateR/r-gmcm-feedstock
          aggregateR/r-gmeta-feedstock
          aggregateR/r-gmse-feedstock
          aggregateR/r-gmwt-feedstock
          aggregateR/r-gnmf-feedstock
          aggregateR/r-goftest-feedstock
          aggregateR/r-goftte-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script0-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script1-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script1-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-googlepolylines-feedstock
          aggregateR/r-gpb-feedstock
          aggregateR/r-gpclib-feedstock
          aggregateR/r-gpg-feedstock
          aggregateR/r-gpvam-feedstock
          aggregateR/r-graphkernels-feedstock
          aggregateR/r-graphlayouts-feedstock
          aggregateR/r-graphql-feedstock
          aggregateR/r-greedyepl-feedstock
          aggregateR/r-greedysbtm-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script1-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script1-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script1-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-googlepolylines-feedstock
          aggregateR/r-gpb-feedstock
          aggregateR/r-gpclib-feedstock
          aggregateR/r-gpg-feedstock
          aggregateR/r-gpvam-feedstock
          aggregateR/r-graphkernels-feedstock
          aggregateR/r-graphlayouts-feedstock
          aggregateR/r-graphql-feedstock
          aggregateR/r-greedyepl-feedstock
          aggregateR/r-greedysbtm-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script1-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script1-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script18-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-googlepolylines-feedstock
          aggregateR/r-gpb-feedstock
          aggregateR/r-gpclib-feedstock
          aggregateR/r-gpg-feedstock
          aggregateR/r-gpvam-feedstock
          aggregateR/r-graphkernels-feedstock
          aggregateR/r-graphlayouts-feedstock
          aggregateR/r-graphql-feedstock
          aggregateR/r-greedyepl-feedstock
          aggregateR/r-greedysbtm-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script1-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script2-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script2-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-grf-feedstock
          aggregateR/r-groupremmap-feedstock
          aggregateR/r-groupsubsetselection-feedstock
          aggregateR/r-grr-feedstock
          aggregateR/r-gscounts-feedstock
          aggregateR/r-gsmoothr-feedstock
          aggregateR/r-gsscopu-feedstock
          aggregateR/r-gte-feedstock
          aggregateR/r-gtfsrouter-feedstock
          aggregateR/r-guts-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script2-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script2-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script2-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-grf-feedstock
          aggregateR/r-groupremmap-feedstock
          aggregateR/r-groupsubsetselection-feedstock
          aggregateR/r-grr-feedstock
          aggregateR/r-gscounts-feedstock
          aggregateR/r-gsmoothr-feedstock
          aggregateR/r-gsscopu-feedstock
          aggregateR/r-gte-feedstock
          aggregateR/r-gtfsrouter-feedstock
          aggregateR/r-guts-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script2-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script2-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script28-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-grf-feedstock
          aggregateR/r-groupremmap-feedstock
          aggregateR/r-groupsubsetselection-feedstock
          aggregateR/r-grr-feedstock
          aggregateR/r-gscounts-feedstock
          aggregateR/r-gsmoothr-feedstock
          aggregateR/r-gsscopu-feedstock
          aggregateR/r-gte-feedstock
          aggregateR/r-gtfsrouter-feedstock
          aggregateR/r-guts-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script2-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script3-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script3-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-gwasexacthw-feedstock
          aggregateR/r-gwfa-feedstock
          aggregateR/r-gyriq-feedstock
          aggregateR/r-h5-feedstock
          aggregateR/r-hapassoc-feedstock
          aggregateR/r-hapsim-feedstock
          aggregateR/r-harmodel-feedstock
          aggregateR/r-hashfunction-feedstock
          aggregateR/r-hashmap-feedstock
          aggregateR/r-hashr-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script3-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script3-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script3-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-gwasexacthw-feedstock
          aggregateR/r-gwfa-feedstock
          aggregateR/r-gyriq-feedstock
          aggregateR/r-h5-feedstock
          aggregateR/r-hapassoc-feedstock
          aggregateR/r-hapsim-feedstock
          aggregateR/r-harmodel-feedstock
          aggregateR/r-hashfunction-feedstock
          aggregateR/r-hashmap-feedstock
          aggregateR/r-hashr-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script3-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script3-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script38-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-gwasexacthw-feedstock
          aggregateR/r-gwfa-feedstock
          aggregateR/r-gyriq-feedstock
          aggregateR/r-h5-feedstock
          aggregateR/r-hapassoc-feedstock
          aggregateR/r-hapsim-feedstock
          aggregateR/r-harmodel-feedstock
          aggregateR/r-hashfunction-feedstock
          aggregateR/r-hashmap-feedstock
          aggregateR/r-hashr-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script3-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script4-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script4-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-hawkes-feedstock
          aggregateR/r-hbmem-feedstock
          aggregateR/r-hbv.ianigla-feedstock
          aggregateR/r-hdbinseg-feedstock
          aggregateR/r-hdbm-feedstock
          aggregateR/r-hdcpdetect-feedstock
          aggregateR/r-hddesign-feedstock
          aggregateR/r-hdf5r-feedstock
          aggregateR/r-hdglm-feedstock
          aggregateR/r-hdlm-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script4-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script4-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script4-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-hawkes-feedstock
          aggregateR/r-hbmem-feedstock
          aggregateR/r-hbv.ianigla-feedstock
          aggregateR/r-hdbinseg-feedstock
          aggregateR/r-hdbm-feedstock
          aggregateR/r-hdcpdetect-feedstock
          aggregateR/r-hddesign-feedstock
          aggregateR/r-hdf5r-feedstock
          aggregateR/r-hdglm-feedstock
          aggregateR/r-hdlm-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script4-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script4-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script48-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-hawkes-feedstock
          aggregateR/r-hbmem-feedstock
          aggregateR/r-hbv.ianigla-feedstock
          aggregateR/r-hdbinseg-feedstock
          aggregateR/r-hdbm-feedstock
          aggregateR/r-hdcpdetect-feedstock
          aggregateR/r-hddesign-feedstock
          aggregateR/r-hdf5r-feedstock
          aggregateR/r-hdglm-feedstock
          aggregateR/r-hdlm-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script4-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script5-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script5-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-hdtweedie-feedstock
          aggregateR/r-heavy-feedstock
          aggregateR/r-hellcor-feedstock
          aggregateR/r-hetgp-feedstock
          aggregateR/r-hgm-feedstock
          aggregateR/r-hhg-feedstock
          aggregateR/r-hi-feedstock
          aggregateR/r-hicseg-feedstock
          aggregateR/r-hiddenmarkov-feedstock
          aggregateR/r-hidimda-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script5-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script5-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script5-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-hdtweedie-feedstock
          aggregateR/r-heavy-feedstock
          aggregateR/r-hellcor-feedstock
          aggregateR/r-hetgp-feedstock
          aggregateR/r-hgm-feedstock
          aggregateR/r-hhg-feedstock
          aggregateR/r-hi-feedstock
          aggregateR/r-hicseg-feedstock
          aggregateR/r-hiddenmarkov-feedstock
          aggregateR/r-hidimda-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script5-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script5-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script58-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-hdtweedie-feedstock
          aggregateR/r-heavy-feedstock
          aggregateR/r-hellcor-feedstock
          aggregateR/r-hetgp-feedstock
          aggregateR/r-hgm-feedstock
          aggregateR/r-hhg-feedstock
          aggregateR/r-hi-feedstock
          aggregateR/r-hicseg-feedstock
          aggregateR/r-hiddenmarkov-feedstock
          aggregateR/r-hidimda-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script5-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script6-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script6-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-hier.part-feedstock
          aggregateR/r-hierband-feedstock
          aggregateR/r-hiernet-feedstock
          aggregateR/r-highlight-feedstock
          aggregateR/r-hillmaker-feedstock
          aggregateR/r-hint-feedstock
          aggregateR/r-histmdl-feedstock
          aggregateR/r-hkevp-feedstock
          aggregateR/r-hlsm-feedstock
          aggregateR/r-hmb-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script6-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script6-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script6-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-hier.part-feedstock
          aggregateR/r-hierband-feedstock
          aggregateR/r-hiernet-feedstock
          aggregateR/r-highlight-feedstock
          aggregateR/r-hillmaker-feedstock
          aggregateR/r-hint-feedstock
          aggregateR/r-histmdl-feedstock
          aggregateR/r-hkevp-feedstock
          aggregateR/r-hlsm-feedstock
          aggregateR/r-hmb-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script6-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script6-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script68-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-hier.part-feedstock
          aggregateR/r-hierband-feedstock
          aggregateR/r-hiernet-feedstock
          aggregateR/r-highlight-feedstock
          aggregateR/r-hillmaker-feedstock
          aggregateR/r-hint-feedstock
          aggregateR/r-histmdl-feedstock
          aggregateR/r-hkevp-feedstock
          aggregateR/r-hlsm-feedstock
          aggregateR/r-hmb-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script6-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script7-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script7-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-hmm.discnp-feedstock
          aggregateR/r-hmmextra0s-feedstock
          aggregateR/r-hogsvdr-feedstock
          aggregateR/r-hommel-feedstock
          aggregateR/r-hpa-feedstock
          aggregateR/r-hqreg-feedstock
          aggregateR/r-hsdm-feedstock
          aggregateR/r-hsm-feedstock
          aggregateR/r-hsmm-feedstock
          aggregateR/r-htdp-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script7-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script7-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script7-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-hmm.discnp-feedstock
          aggregateR/r-hmmextra0s-feedstock
          aggregateR/r-hogsvdr-feedstock
          aggregateR/r-hommel-feedstock
          aggregateR/r-hpa-feedstock
          aggregateR/r-hqreg-feedstock
          aggregateR/r-hsdm-feedstock
          aggregateR/r-hsm-feedstock
          aggregateR/r-hsmm-feedstock
          aggregateR/r-htdp-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script7-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script7-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script78-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-hmm.discnp-feedstock
          aggregateR/r-hmmextra0s-feedstock
          aggregateR/r-hogsvdr-feedstock
          aggregateR/r-hommel-feedstock
          aggregateR/r-hpa-feedstock
          aggregateR/r-hqreg-feedstock
          aggregateR/r-hsdm-feedstock
          aggregateR/r-hsm-feedstock
          aggregateR/r-hsmm-feedstock
          aggregateR/r-htdp-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script7-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script8-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script8-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-htmltidy-feedstock
          aggregateR/r-htree-feedstock
          aggregateR/r-huge-feedstock
          aggregateR/r-humaniformat-feedstock
          aggregateR/r-humanleague-feedstock
          aggregateR/r-hwxtest-feedstock
          aggregateR/r-hybridmc-feedstock
          aggregateR/r-hypergea-feedstock
          aggregateR/r-ibdreg-feedstock
          aggregateR/r-ibm-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script8-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script8-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script8-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-htmltidy-feedstock
          aggregateR/r-htree-feedstock
          aggregateR/r-huge-feedstock
          aggregateR/r-humaniformat-feedstock
          aggregateR/r-humanleague-feedstock
          aggregateR/r-hwxtest-feedstock
          aggregateR/r-hybridmc-feedstock
          aggregateR/r-hypergea-feedstock
          aggregateR/r-ibdreg-feedstock
          aggregateR/r-ibm-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script8-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script8-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script88-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-htmltidy-feedstock
          aggregateR/r-htree-feedstock
          aggregateR/r-huge-feedstock
          aggregateR/r-humaniformat-feedstock
          aggregateR/r-humanleague-feedstock
          aggregateR/r-hwxtest-feedstock
          aggregateR/r-hybridmc-feedstock
          aggregateR/r-hypergea-feedstock
          aggregateR/r-ibdreg-feedstock
          aggregateR/r-ibm-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script8-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script9-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script9-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-ibmcraftr-feedstock
          aggregateR/r-iboot-feedstock
          aggregateR/r-ibr-feedstock
          aggregateR/r-ibs-feedstock
          aggregateR/r-ibst-feedstock
          aggregateR/r-icamix-feedstock
          aggregateR/r-icapca-feedstock
          aggregateR/r-iccbeta-feedstock
          aggregateR/r-icd-feedstock
          aggregateR/r-ice-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script9-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script9-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script9-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-ibmcraftr-feedstock
          aggregateR/r-iboot-feedstock
          aggregateR/r-ibr-feedstock
          aggregateR/r-ibs-feedstock
          aggregateR/r-ibst-feedstock
          aggregateR/r-icamix-feedstock
          aggregateR/r-icapca-feedstock
          aggregateR/r-iccbeta-feedstock
          aggregateR/r-icd-feedstock
          aggregateR/r-ice-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script9-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script9-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script98-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-ibmcraftr-feedstock
          aggregateR/r-iboot-feedstock
          aggregateR/r-ibr-feedstock
          aggregateR/r-ibs-feedstock
          aggregateR/r-ibst-feedstock
          aggregateR/r-icamix-feedstock
          aggregateR/r-icapca-feedstock
          aggregateR/r-iccbeta-feedstock
          aggregateR/r-icd-feedstock
          aggregateR/r-ice-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script9-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
