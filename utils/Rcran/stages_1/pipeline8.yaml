groups: []
resources:
- name: rsync-build-pack
  type: rsync-resource
  source:
    base_dir: /ci/build_pack
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script0-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script0-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script0-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script1-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script1-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script1-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script2-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script2-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script2-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script3-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script3-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script3-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script4-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script4-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script4-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script5-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script5-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script5-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script6-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script6-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script6-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script7-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script7-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script7-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script8-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script8-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script8-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script9-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script9-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script9-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
resource_types:
- name: rsync-resource
  type: docker-image
  source:
    repository: conda/concourse-rsync-resource
    tag: latest
jobs:
- name: build_r_script0-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script0-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-natural-feedstock
          aggregateR/r-ncdf4-feedstock
          aggregateR/r-ncpen-feedstock
          aggregateR/r-ncvreg-feedstock
          aggregateR/r-neariso-feedstock
          aggregateR/r-negenes-feedstock
          aggregateR/r-netchain-feedstock
          aggregateR/r-netcoh-feedstock
          aggregateR/r-netcomp-feedstock
          aggregateR/r-netrankr-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script0-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script0-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script0-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-natural-feedstock
          aggregateR/r-ncdf4-feedstock
          aggregateR/r-ncpen-feedstock
          aggregateR/r-ncvreg-feedstock
          aggregateR/r-neariso-feedstock
          aggregateR/r-negenes-feedstock
          aggregateR/r-netchain-feedstock
          aggregateR/r-netcoh-feedstock
          aggregateR/r-netcomp-feedstock
          aggregateR/r-netrankr-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script0-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script0-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script08-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-natural-feedstock
          aggregateR/r-ncdf4-feedstock
          aggregateR/r-ncpen-feedstock
          aggregateR/r-ncvreg-feedstock
          aggregateR/r-neariso-feedstock
          aggregateR/r-negenes-feedstock
          aggregateR/r-netchain-feedstock
          aggregateR/r-netcoh-feedstock
          aggregateR/r-netcomp-feedstock
          aggregateR/r-netrankr-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script0-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script1-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script1-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-nets-feedstock
          aggregateR/r-neurosim-feedstock
          aggregateR/r-ngram-feedstock
          aggregateR/r-ngspatial-feedstock
          aggregateR/r-nice-feedstock
          aggregateR/r-nimble-feedstock
          aggregateR/r-nleqslv-feedstock
          aggregateR/r-nltm-feedstock
          aggregateR/r-nmfgpu4r-feedstock
          aggregateR/r-nnlm-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script1-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script1-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script1-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-nets-feedstock
          aggregateR/r-neurosim-feedstock
          aggregateR/r-ngram-feedstock
          aggregateR/r-ngspatial-feedstock
          aggregateR/r-nice-feedstock
          aggregateR/r-nimble-feedstock
          aggregateR/r-nleqslv-feedstock
          aggregateR/r-nltm-feedstock
          aggregateR/r-nmfgpu4r-feedstock
          aggregateR/r-nnlm-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script1-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script1-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script18-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-nets-feedstock
          aggregateR/r-neurosim-feedstock
          aggregateR/r-ngram-feedstock
          aggregateR/r-ngspatial-feedstock
          aggregateR/r-nice-feedstock
          aggregateR/r-nimble-feedstock
          aggregateR/r-nleqslv-feedstock
          aggregateR/r-nltm-feedstock
          aggregateR/r-nmfgpu4r-feedstock
          aggregateR/r-nnlm-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script1-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script2-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script2-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-nnls-feedstock
          aggregateR/r-noncompliance-feedstock
          aggregateR/r-nonneg.cg-feedstock
          aggregateR/r-nonpmodelcheck-feedstock
          aggregateR/r-nopaco-feedstock
          aggregateR/r-norm-feedstock
          aggregateR/r-norm2-feedstock
          aggregateR/r-normt3-feedstock
          aggregateR/r-nortestarma-feedstock
          aggregateR/r-not-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script2-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script2-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script2-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-nnls-feedstock
          aggregateR/r-noncompliance-feedstock
          aggregateR/r-nonneg.cg-feedstock
          aggregateR/r-nonpmodelcheck-feedstock
          aggregateR/r-nopaco-feedstock
          aggregateR/r-norm-feedstock
          aggregateR/r-norm2-feedstock
          aggregateR/r-normt3-feedstock
          aggregateR/r-nortestarma-feedstock
          aggregateR/r-not-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script2-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script2-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script28-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-nnls-feedstock
          aggregateR/r-noncompliance-feedstock
          aggregateR/r-nonneg.cg-feedstock
          aggregateR/r-nonpmodelcheck-feedstock
          aggregateR/r-nopaco-feedstock
          aggregateR/r-norm-feedstock
          aggregateR/r-norm2-feedstock
          aggregateR/r-normt3-feedstock
          aggregateR/r-nortestarma-feedstock
          aggregateR/r-not-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script2-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script3-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script3-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-np-feedstock
          aggregateR/r-npbayesimputecat-feedstock
          aggregateR/r-npcoptest-feedstock
          aggregateR/r-npcp-feedstock
          aggregateR/r-nprocregression-feedstock
          aggregateR/r-npsimex-feedstock
          aggregateR/r-npst-feedstock
          aggregateR/r-nscluster-feedstock
          aggregateR/r-nseval-feedstock
          aggregateR/r-nsrfa-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script3-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script3-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script3-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-np-feedstock
          aggregateR/r-npbayesimputecat-feedstock
          aggregateR/r-npcoptest-feedstock
          aggregateR/r-npcp-feedstock
          aggregateR/r-nprocregression-feedstock
          aggregateR/r-npsimex-feedstock
          aggregateR/r-npst-feedstock
          aggregateR/r-nscluster-feedstock
          aggregateR/r-nseval-feedstock
          aggregateR/r-nsrfa-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script3-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script3-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script38-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-np-feedstock
          aggregateR/r-npbayesimputecat-feedstock
          aggregateR/r-npcoptest-feedstock
          aggregateR/r-npcp-feedstock
          aggregateR/r-nprocregression-feedstock
          aggregateR/r-npsimex-feedstock
          aggregateR/r-npst-feedstock
          aggregateR/r-nscluster-feedstock
          aggregateR/r-nseval-feedstock
          aggregateR/r-nsrfa-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script3-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script4-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script4-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-numero-feedstock
          aggregateR/r-numosl-feedstock
          aggregateR/r-nvennr-feedstock
          aggregateR/r-oaqc-feedstock
          aggregateR/r-obliclus-feedstock
          aggregateR/r-obsmd-feedstock
          aggregateR/r-odeintr-feedstock
          aggregateR/r-oem-feedstock
          aggregateR/r-officer-feedstock
          aggregateR/r-offlinechange-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script4-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script4-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script4-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-numero-feedstock
          aggregateR/r-numosl-feedstock
          aggregateR/r-nvennr-feedstock
          aggregateR/r-oaqc-feedstock
          aggregateR/r-obliclus-feedstock
          aggregateR/r-obsmd-feedstock
          aggregateR/r-odeintr-feedstock
          aggregateR/r-oem-feedstock
          aggregateR/r-officer-feedstock
          aggregateR/r-offlinechange-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script4-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script4-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script48-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-numero-feedstock
          aggregateR/r-numosl-feedstock
          aggregateR/r-nvennr-feedstock
          aggregateR/r-oaqc-feedstock
          aggregateR/r-obliclus-feedstock
          aggregateR/r-obsmd-feedstock
          aggregateR/r-odeintr-feedstock
          aggregateR/r-oem-feedstock
          aggregateR/r-officer-feedstock
          aggregateR/r-offlinechange-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script4-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script5-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script5-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-olctools-feedstock
          aggregateR/r-onearmphasetwostudy-feedstock
          aggregateR/r-onehot-feedstock
          aggregateR/r-onion-feedstock
          aggregateR/r-onlinevar-feedstock
          aggregateR/r-opart-feedstock
          aggregateR/r-opencl-feedstock
          aggregateR/r-opencv-feedstock
          aggregateR/r-openmpcontroller-feedstock
          aggregateR/r-optgs-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script5-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script5-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script5-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-olctools-feedstock
          aggregateR/r-onearmphasetwostudy-feedstock
          aggregateR/r-onehot-feedstock
          aggregateR/r-onion-feedstock
          aggregateR/r-onlinevar-feedstock
          aggregateR/r-opart-feedstock
          aggregateR/r-opencl-feedstock
          aggregateR/r-opencv-feedstock
          aggregateR/r-openmpcontroller-feedstock
          aggregateR/r-optgs-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script5-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script5-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script58-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-olctools-feedstock
          aggregateR/r-onearmphasetwostudy-feedstock
          aggregateR/r-onehot-feedstock
          aggregateR/r-onion-feedstock
          aggregateR/r-onlinevar-feedstock
          aggregateR/r-opart-feedstock
          aggregateR/r-opencl-feedstock
          aggregateR/r-opencv-feedstock
          aggregateR/r-openmpcontroller-feedstock
          aggregateR/r-optgs-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script5-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script6-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script6-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-opthedging-feedstock
          aggregateR/r-optimization-feedstock
          aggregateR/r-opusminer-feedstock
          aggregateR/r-orca-feedstock
          aggregateR/r-ordinalclust-feedstock
          aggregateR/r-ordinalgmifs-feedstock
          aggregateR/r-ore-feedstock
          aggregateR/r-ormdr-feedstock
          aggregateR/r-orsk-feedstock
          aggregateR/r-osc-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script6-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script6-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script6-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-opthedging-feedstock
          aggregateR/r-optimization-feedstock
          aggregateR/r-opusminer-feedstock
          aggregateR/r-orca-feedstock
          aggregateR/r-ordinalclust-feedstock
          aggregateR/r-ordinalgmifs-feedstock
          aggregateR/r-ore-feedstock
          aggregateR/r-ormdr-feedstock
          aggregateR/r-orsk-feedstock
          aggregateR/r-osc-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script6-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script6-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script68-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-opthedging-feedstock
          aggregateR/r-optimization-feedstock
          aggregateR/r-opusminer-feedstock
          aggregateR/r-orca-feedstock
          aggregateR/r-ordinalclust-feedstock
          aggregateR/r-ordinalgmifs-feedstock
          aggregateR/r-ore-feedstock
          aggregateR/r-ormdr-feedstock
          aggregateR/r-orsk-feedstock
          aggregateR/r-osc-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script6-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script7-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script7-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-osdesign-feedstock
          aggregateR/r-osqp-feedstock
          aggregateR/r-overlap-feedstock
          aggregateR/r-pacbo-feedstock
          aggregateR/r-packcircles-feedstock
          aggregateR/r-pamr-feedstock
          aggregateR/r-pan-feedstock
          aggregateR/r-pander-feedstock
          aggregateR/r-parglm-feedstock
          aggregateR/r-parmigene-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script7-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script7-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script7-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-osdesign-feedstock
          aggregateR/r-osqp-feedstock
          aggregateR/r-overlap-feedstock
          aggregateR/r-pacbo-feedstock
          aggregateR/r-packcircles-feedstock
          aggregateR/r-pamr-feedstock
          aggregateR/r-pan-feedstock
          aggregateR/r-pander-feedstock
          aggregateR/r-parglm-feedstock
          aggregateR/r-parmigene-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script7-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script7-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script78-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-osdesign-feedstock
          aggregateR/r-osqp-feedstock
          aggregateR/r-overlap-feedstock
          aggregateR/r-pacbo-feedstock
          aggregateR/r-packcircles-feedstock
          aggregateR/r-pamr-feedstock
          aggregateR/r-pan-feedstock
          aggregateR/r-pander-feedstock
          aggregateR/r-parglm-feedstock
          aggregateR/r-parmigene-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script7-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script8-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script8-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-party-feedstock
          aggregateR/r-patchdvi-feedstock
          aggregateR/r-pathmapping-feedstock
          aggregateR/r-pawls-feedstock
          aggregateR/r-pbdncdf4-feedstock
          aggregateR/r-pbdprof-feedstock
          aggregateR/r-pbdrpc-feedstock
          aggregateR/r-pbmcapply-feedstock
          aggregateR/r-pbrf-feedstock
          aggregateR/r-pbsddesolve-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script8-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script8-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script8-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-party-feedstock
          aggregateR/r-patchdvi-feedstock
          aggregateR/r-pathmapping-feedstock
          aggregateR/r-pawls-feedstock
          aggregateR/r-pbdncdf4-feedstock
          aggregateR/r-pbdprof-feedstock
          aggregateR/r-pbdrpc-feedstock
          aggregateR/r-pbmcapply-feedstock
          aggregateR/r-pbrf-feedstock
          aggregateR/r-pbsddesolve-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script8-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script8-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script88-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-party-feedstock
          aggregateR/r-patchdvi-feedstock
          aggregateR/r-pathmapping-feedstock
          aggregateR/r-pawls-feedstock
          aggregateR/r-pbdncdf4-feedstock
          aggregateR/r-pbdprof-feedstock
          aggregateR/r-pbdrpc-feedstock
          aggregateR/r-pbmcapply-feedstock
          aggregateR/r-pbrf-feedstock
          aggregateR/r-pbsddesolve-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script8-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script9-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script9-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-pbsmapping-feedstock
          aggregateR/r-pbsmodelling-feedstock
          aggregateR/r-pbv-feedstock
          aggregateR/r-pca4ts-feedstock
          aggregateR/r-pcal1-feedstock
          aggregateR/r-pcict-feedstock
          aggregateR/r-pcirt-feedstock
          aggregateR/r-pcit-feedstock
          aggregateR/r-pdc-feedstock
          aggregateR/r-pdfestimator-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script9-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script9-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script9-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-pbsmapping-feedstock
          aggregateR/r-pbsmodelling-feedstock
          aggregateR/r-pbv-feedstock
          aggregateR/r-pca4ts-feedstock
          aggregateR/r-pcal1-feedstock
          aggregateR/r-pcict-feedstock
          aggregateR/r-pcirt-feedstock
          aggregateR/r-pcit-feedstock
          aggregateR/r-pdc-feedstock
          aggregateR/r-pdfestimator-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script9-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script9-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script98-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-pbsmapping-feedstock
          aggregateR/r-pbsmodelling-feedstock
          aggregateR/r-pbv-feedstock
          aggregateR/r-pca4ts-feedstock
          aggregateR/r-pcal1-feedstock
          aggregateR/r-pcict-feedstock
          aggregateR/r-pcirt-feedstock
          aggregateR/r-pcit-feedstock
          aggregateR/r-pdc-feedstock
          aggregateR/r-pdfestimator-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script9-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
