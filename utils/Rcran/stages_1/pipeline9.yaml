groups: []
resources:
- name: rsync-build-pack
  type: rsync-resource
  source:
    base_dir: /ci/build_pack
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script0-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script0-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script0-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script1-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script1-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script1-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script2-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script2-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script2-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script3-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script3-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script3-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script4-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script4-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script4-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script5-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script5-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script5-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script6-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script6-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script6-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script7-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script7-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script7-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script8-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script8-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script8-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script9-on-linux_64
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script9-on-osx
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
- name: rsync_build_r_script9-on-winbuilder
  type: rsync-resource
  source:
    base_dir: /ci/ktietz/artifacts
    disable_version_path: true
    private_key: ((common.intermediate-private-key))
    server: bremen.corp.continuum.io
    user: ci
resource_types:
- name: rsync-resource
  type: docker-image
  source:
    repository: conda/concourse-rsync-resource
    tag: latest
jobs:
- name: build_r_script0-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script0-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-pdist-feedstock
          aggregateR/r-pdsce-feedstock
          aggregateR/r-peakerror-feedstock
          aggregateR/r-peaksegdisk-feedstock
          aggregateR/r-peaksegdp-feedstock
          aggregateR/r-pearsonds-feedstock
          aggregateR/r-pedigreemm-feedstock
          aggregateR/r-pedigreetools-feedstock
          aggregateR/r-pedometrics-feedstock
          aggregateR/r-penalized-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script0-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script0-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script0-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-pdist-feedstock
          aggregateR/r-pdsce-feedstock
          aggregateR/r-peakerror-feedstock
          aggregateR/r-peaksegdisk-feedstock
          aggregateR/r-peaksegdp-feedstock
          aggregateR/r-pearsonds-feedstock
          aggregateR/r-pedigreemm-feedstock
          aggregateR/r-pedigreetools-feedstock
          aggregateR/r-pedometrics-feedstock
          aggregateR/r-penalized-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script0-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script0-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script08-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-pdist-feedstock
          aggregateR/r-pdsce-feedstock
          aggregateR/r-peakerror-feedstock
          aggregateR/r-peaksegdisk-feedstock
          aggregateR/r-peaksegdp-feedstock
          aggregateR/r-pearsonds-feedstock
          aggregateR/r-pedigreemm-feedstock
          aggregateR/r-pedigreetools-feedstock
          aggregateR/r-pedometrics-feedstock
          aggregateR/r-penalized-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script0-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script1-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script1-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-pencoxfrail-feedstock
          aggregateR/r-penmsm-feedstock
          aggregateR/r-pense-feedstock
          aggregateR/r-peptides-feedstock
          aggregateR/r-perccal-feedstock
          aggregateR/r-performanceanalytics-feedstock
          aggregateR/r-permallows-feedstock
          aggregateR/r-permdep-feedstock
          aggregateR/r-pet-feedstock
          aggregateR/r-pgam-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script1-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script1-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script1-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-pencoxfrail-feedstock
          aggregateR/r-penmsm-feedstock
          aggregateR/r-pense-feedstock
          aggregateR/r-peptides-feedstock
          aggregateR/r-perccal-feedstock
          aggregateR/r-performanceanalytics-feedstock
          aggregateR/r-permallows-feedstock
          aggregateR/r-permdep-feedstock
          aggregateR/r-pet-feedstock
          aggregateR/r-pgam-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script1-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script1-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script18-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-pencoxfrail-feedstock
          aggregateR/r-penmsm-feedstock
          aggregateR/r-pense-feedstock
          aggregateR/r-peptides-feedstock
          aggregateR/r-perccal-feedstock
          aggregateR/r-performanceanalytics-feedstock
          aggregateR/r-permallows-feedstock
          aggregateR/r-permdep-feedstock
          aggregateR/r-pet-feedstock
          aggregateR/r-pgam-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script1-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script2-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script2-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-pgbart-feedstock
          aggregateR/r-pgdraw-feedstock
          aggregateR/r-pgee.mixed-feedstock
          aggregateR/r-pgmm-feedstock
          aggregateR/r-pgsc-feedstock
          aggregateR/r-ph2bayes-feedstock
          aggregateR/r-ph2hetero-feedstock
          aggregateR/r-phase123-feedstock
          aggregateR/r-phenex-feedstock
          aggregateR/r-pheno-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script2-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script2-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script2-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-pgbart-feedstock
          aggregateR/r-pgdraw-feedstock
          aggregateR/r-pgee.mixed-feedstock
          aggregateR/r-pgmm-feedstock
          aggregateR/r-pgsc-feedstock
          aggregateR/r-ph2bayes-feedstock
          aggregateR/r-ph2hetero-feedstock
          aggregateR/r-phase123-feedstock
          aggregateR/r-phenex-feedstock
          aggregateR/r-pheno-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script2-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script2-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script28-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-pgbart-feedstock
          aggregateR/r-pgdraw-feedstock
          aggregateR/r-pgee.mixed-feedstock
          aggregateR/r-pgmm-feedstock
          aggregateR/r-pgsc-feedstock
          aggregateR/r-ph2bayes-feedstock
          aggregateR/r-ph2hetero-feedstock
          aggregateR/r-phase123-feedstock
          aggregateR/r-phenex-feedstock
          aggregateR/r-pheno-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script2-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script3-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script3-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-phmm-feedstock
          aggregateR/r-phonics-feedstock
          aggregateR/r-phreeqc-feedstock
          aggregateR/r-phyclust-feedstock
          aggregateR/r-phyloland-feedstock
          aggregateR/r-phylomeasures-feedstock
          aggregateR/r-physiology-feedstock
          aggregateR/r-picasso-feedstock
          aggregateR/r-pieceexpintensity-feedstock
          aggregateR/r-pijavski-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script3-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script3-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script3-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-phmm-feedstock
          aggregateR/r-phonics-feedstock
          aggregateR/r-phreeqc-feedstock
          aggregateR/r-phyclust-feedstock
          aggregateR/r-phyloland-feedstock
          aggregateR/r-phylomeasures-feedstock
          aggregateR/r-physiology-feedstock
          aggregateR/r-picasso-feedstock
          aggregateR/r-pieceexpintensity-feedstock
          aggregateR/r-pijavski-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script3-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script3-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script38-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-phmm-feedstock
          aggregateR/r-phonics-feedstock
          aggregateR/r-phreeqc-feedstock
          aggregateR/r-phyclust-feedstock
          aggregateR/r-phyloland-feedstock
          aggregateR/r-phylomeasures-feedstock
          aggregateR/r-physiology-feedstock
          aggregateR/r-picasso-feedstock
          aggregateR/r-pieceexpintensity-feedstock
          aggregateR/r-pijavski-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script3-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script4-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script4-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-pingr-feedstock
          aggregateR/r-piton-feedstock
          aggregateR/r-pivotalr-feedstock
          aggregateR/r-plac-feedstock
          aggregateR/r-planor-feedstock
          aggregateR/r-playerratings-feedstock
          aggregateR/r-plfm-feedstock
          aggregateR/r-pliable-feedstock
          aggregateR/r-pln-feedstock
          aggregateR/r-plordprob-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script4-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script4-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script4-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-pingr-feedstock
          aggregateR/r-piton-feedstock
          aggregateR/r-pivotalr-feedstock
          aggregateR/r-plac-feedstock
          aggregateR/r-planor-feedstock
          aggregateR/r-playerratings-feedstock
          aggregateR/r-plfm-feedstock
          aggregateR/r-pliable-feedstock
          aggregateR/r-pln-feedstock
          aggregateR/r-plordprob-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script4-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script4-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script48-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-pingr-feedstock
          aggregateR/r-piton-feedstock
          aggregateR/r-pivotalr-feedstock
          aggregateR/r-plac-feedstock
          aggregateR/r-planor-feedstock
          aggregateR/r-playerratings-feedstock
          aggregateR/r-plfm-feedstock
          aggregateR/r-pliable-feedstock
          aggregateR/r-pln-feedstock
          aggregateR/r-plordprob-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script4-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script5-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script5-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-plugdensity-feedstock
          aggregateR/r-poibin-feedstock
          aggregateR/r-poilog-feedstock
          aggregateR/r-poisbinom-feedstock
          aggregateR/r-poisdoublesamp-feedstock
          aggregateR/r-poissonpca-feedstock
          aggregateR/r-polyclip-feedstock
          aggregateR/r-polycub-feedstock
          aggregateR/r-polyfreqs-feedstock
          aggregateR/r-polylabelr-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script5-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script5-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script5-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-plugdensity-feedstock
          aggregateR/r-poibin-feedstock
          aggregateR/r-poilog-feedstock
          aggregateR/r-poisbinom-feedstock
          aggregateR/r-poisdoublesamp-feedstock
          aggregateR/r-poissonpca-feedstock
          aggregateR/r-polyclip-feedstock
          aggregateR/r-polycub-feedstock
          aggregateR/r-polyfreqs-feedstock
          aggregateR/r-polylabelr-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script5-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script5-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script58-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-plugdensity-feedstock
          aggregateR/r-poibin-feedstock
          aggregateR/r-poilog-feedstock
          aggregateR/r-poisbinom-feedstock
          aggregateR/r-poisdoublesamp-feedstock
          aggregateR/r-poissonpca-feedstock
          aggregateR/r-polyclip-feedstock
          aggregateR/r-polycub-feedstock
          aggregateR/r-polyfreqs-feedstock
          aggregateR/r-polylabelr-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script5-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script6-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script6-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-polynomf-feedstock
          aggregateR/r-polysat-feedstock
          aggregateR/r-pomaspu-feedstock
          aggregateR/r-pomdp-feedstock
          aggregateR/r-pomp-feedstock
          aggregateR/r-pooh-feedstock
          aggregateR/r-pop.wolf-feedstock
          aggregateR/r-popkin-feedstock
          aggregateR/r-population-feedstock
          aggregateR/r-pot-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script6-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script6-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script6-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-polynomf-feedstock
          aggregateR/r-polysat-feedstock
          aggregateR/r-pomaspu-feedstock
          aggregateR/r-pomdp-feedstock
          aggregateR/r-pomp-feedstock
          aggregateR/r-pooh-feedstock
          aggregateR/r-pop.wolf-feedstock
          aggregateR/r-popkin-feedstock
          aggregateR/r-population-feedstock
          aggregateR/r-pot-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script6-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script6-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script68-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-polynomf-feedstock
          aggregateR/r-polysat-feedstock
          aggregateR/r-pomaspu-feedstock
          aggregateR/r-pomdp-feedstock
          aggregateR/r-pomp-feedstock
          aggregateR/r-pooh-feedstock
          aggregateR/r-pop.wolf-feedstock
          aggregateR/r-popkin-feedstock
          aggregateR/r-population-feedstock
          aggregateR/r-pot-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script6-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script7-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script7-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-potts-feedstock
          aggregateR/r-power-feedstock
          aggregateR/r-pp-feedstock
          aggregateR/r-pp3-feedstock
          aggregateR/r-ppcc-feedstock
          aggregateR/r-ppmr-feedstock
          aggregateR/r-pqlseq-feedstock
          aggregateR/r-praznik-feedstock
          aggregateR/r-prclust-feedstock
          aggregateR/r-precisesums-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script7-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script7-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script7-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-potts-feedstock
          aggregateR/r-power-feedstock
          aggregateR/r-pp-feedstock
          aggregateR/r-pp3-feedstock
          aggregateR/r-ppcc-feedstock
          aggregateR/r-ppmr-feedstock
          aggregateR/r-pqlseq-feedstock
          aggregateR/r-praznik-feedstock
          aggregateR/r-prclust-feedstock
          aggregateR/r-precisesums-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script7-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script7-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script78-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-potts-feedstock
          aggregateR/r-power-feedstock
          aggregateR/r-pp-feedstock
          aggregateR/r-pp3-feedstock
          aggregateR/r-ppcc-feedstock
          aggregateR/r-ppmr-feedstock
          aggregateR/r-pqlseq-feedstock
          aggregateR/r-praznik-feedstock
          aggregateR/r-prclust-feedstock
          aggregateR/r-precisesums-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script7-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script8-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script8-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-predkmeans-feedstock
          aggregateR/r-primes-feedstock
          aggregateR/r-primme-feedstock
          aggregateR/r-princurve-feedstock
          aggregateR/r-profdpm-feedstock
          aggregateR/r-proj4-feedstock
          aggregateR/r-prospectr-feedstock
          aggregateR/r-protoclust-feedstock
          aggregateR/r-protolite-feedstock
          aggregateR/r-protviz-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script8-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script8-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script8-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-predkmeans-feedstock
          aggregateR/r-primes-feedstock
          aggregateR/r-primme-feedstock
          aggregateR/r-princurve-feedstock
          aggregateR/r-profdpm-feedstock
          aggregateR/r-proj4-feedstock
          aggregateR/r-prospectr-feedstock
          aggregateR/r-protoclust-feedstock
          aggregateR/r-protolite-feedstock
          aggregateR/r-protviz-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script8-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script8-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script88-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-predkmeans-feedstock
          aggregateR/r-primes-feedstock
          aggregateR/r-primme-feedstock
          aggregateR/r-princurve-feedstock
          aggregateR/r-profdpm-feedstock
          aggregateR/r-proj4-feedstock
          aggregateR/r-prospectr-feedstock
          aggregateR/r-protoclust-feedstock
          aggregateR/r-protolite-feedstock
          aggregateR/r-protviz-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script8-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script9-on-linux_64
  plan:
  - task: build
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: conda/c3i-linux-64
          tag: latest
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - conda update -y conda-build&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x &&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --R 3.6.1 -c local -c r_test
          --output-folder=output-artifacts --cache-dir=output-source --stats-file=stats/build_r_script9-on-linux_64_1564756033.json
          --skip-existing --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-proxy-feedstock
          aggregateR/r-prsr-feedstock
          aggregateR/r-pscl-feedstock
          aggregateR/r-pspearman-feedstock
          aggregateR/r-psychotools-feedstock
          aggregateR/r-ptest-feedstock
          aggregateR/r-pts2polys-feedstock
          aggregateR/r-ptsuite-feedstock
          aggregateR/r-ptw-feedstock
          aggregateR/r-pullword-feedstock
          
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script9-on-linux_64
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script9-on-osx
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - osx_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: darwin
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: sh
        args:
        - -exc
        - hostname&& pwd&& mkdir build_env&& unzip -o -q rsync-build-pack/osx_build_env_latest.zip
          -d build_env&& source build_env/bin/activate&& conda-unpack&& conda init&&
          source build_env/etc/profile.d/conda.sh&& conda config --set add_pip_as_python_dependency
          False&& conda config --add default_channels https://repo.anaconda.com/pkgs/main&&
          conda config --add default_channels https://repo.anaconda.com/pkgs/r&& conda
          info&& set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          set +x&& echo machine github.com login $GITHUB_USER password $GITHUB_TOKEN
          protocol https > ~/.netrc&& set -x&& set +x&& echo machine github.com login
          $GITHUB_USER password $GITHUB_TOKEN protocol https > ~/.netrc&& set -x&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-anaconda-upload --error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script9-on-osx_1564756033.json
          --skip-existing -c local -c r_test --R 3.6.1 --croot . -m ./aggregateR/conda_build_config.yaml
          aggregateR/r-proxy-feedstock
          aggregateR/r-prsr-feedstock
          aggregateR/r-pscl-feedstock
          aggregateR/r-pspearman-feedstock
          aggregateR/r-psychotools-feedstock
          aggregateR/r-ptest-feedstock
          aggregateR/r-pts2polys-feedstock
          aggregateR/r-ptsuite-feedstock
          aggregateR/r-ptw-feedstock
          aggregateR/r-pullword-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script9-on-osx
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
- name: build_r_script9-on-winbuilder
  plan:
  - get: rsync-build-pack
    params:
      rsync_opts:
      - --include
      - windows_build_env_latest.zip
      - --exclude
      - '*'
      - -v
  - task: build
    config:
      platform: windows
      params:
        GITHUB_TOKEN: ((common.recipe-repo-access-token))
        GITHUB_USER: SA-PCR-RO
      run:
        path: cmd.exe
        args:
        - /c
        - hostname&& mkdir build_env&& echo %CD%&& echo Extracting build environment&&
          7z x ./rsync-build-pack/windows_build_env_latest.zip -o./build_env -y&&
          echo Activating build environment&& call .\build_env\Scripts\activate&&
          echo Unpacking environment&& conda-unpack&& conda config --system --set
          add_pip_as_python_dependency False&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/main&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/r&& conda config --system --add default_channels
          https://repo.anaconda.com/pkgs/msys2&& conda info&& (echo machine github.com
          login %GITHUB_USER% password %GITHUB_TOKEN% protocol https > %USERPROFILE%\_netrc
          || exit 0)&& (echo machine github.com login %GITHUB_USER% password %GITHUB_TOKEN%
          protocol https > %USERPROFILE%\_netrc || exit 0)&&
          git clone https://github.com/AnacondaRecipes/aggregateR.git &&
          cd aggregateR && git checkout latest_update && cd .. &&
          conda update -y -n base conda && conda update -y --all &&
          conda-build --no-test --no-anaconda-upload --no-error-overlinking --output-folder=output-artifacts
          --cache-dir=output-source --stats-file=stats/build_r_script98-on-winbuilder_1564756033.json
          --croot C:\ci --skip-existing --R 3.6.1 -c local -c r_test -m aggregateR/conda_build_config.yaml
          aggregateR/r-proxy-feedstock
          aggregateR/r-prsr-feedstock
          aggregateR/r-pscl-feedstock
          aggregateR/r-pspearman-feedstock
          aggregateR/r-psychotools-feedstock
          aggregateR/r-ptest-feedstock
          aggregateR/r-pts2polys-feedstock
          aggregateR/r-ptsuite-feedstock
          aggregateR/r-ptw-feedstock
          aggregateR/r-pullword-feedstock
          
      inputs:
      - name: rsync-build-pack
      outputs:
      - name: output-artifacts
      - name: output-source
      - name: stats
  - put: rsync_build_r_script9-on-winbuilder
    params:
      rsync_opts:
      - --archive
      - --no-perms
      - --omit-dir-times
      - --verbose
      - --exclude
      - '"**/*.json*"'
      - --exclude
      - '"**/*.*ml"'
      - --exclude
      - '"**/.cache"'
      sync_dir: output-artifacts
    get_params:
      skip_download: true
